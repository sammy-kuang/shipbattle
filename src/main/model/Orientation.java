package model;

// Represents the orientation of a ship on the board
// For example, a LeftRight ship at (0,0) of length 4
// spans the x-axis from 0 to 3 on y = 0
public enum Orientation {
    LeftRight,
    UpDown
}
